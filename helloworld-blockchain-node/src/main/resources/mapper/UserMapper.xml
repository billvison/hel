<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xingkaichun.helloworldblockchain.node.dao.UserDao" >

    <resultMap id="BaseResultMap" type="com.xingkaichun.helloworldblockchain.node.model.UserEntity" >
        <id column="userName" property="userName" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        userName, password
    </sql>

    <select id="queryAllUser" resultMap="BaseResultMap">
        select * from user
    </select>

    <select id="queryUserByUserName" resultMap="BaseResultMap">
        select *
        from user where userName = #{userName,jdbcType=VARCHAR}
    </select>

    <insert id="addUser" parameterType="com.xingkaichun.helloworldblockchain.node.model.UserEntity">
        INSERT INTO user (userName, password)
        VALUES (#{userName,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR})
    </insert>

    <delete id="deleteAllUser">
        delete from user
    </delete>
</mapper>